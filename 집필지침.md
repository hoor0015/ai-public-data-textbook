# 집필지침 (모든 장 공통)

이 문서는 「AI 기반 공공데이터 분석」 교재 26개 장의 집필 기준이다. 모든 장은 이 지침을 따른다.

## 1. 독자와 눈높이

- **독자: 문과(행정학과) 학부 1학년.** 프로그래밍·통계 사전지식 없음을 가정한다.
- 모든 개념은 일상 비유와 구체적 예시로 먼저 설명하고, 그다음 용어를 붙인다. 예시 없이 개념만 서술하는 문단을 만들지 않는다.
- 영어 용어는 처음 나올 때 "한글 용어(english term)" 형태로 한 번만 병기하고 이후 한글로 쓴다.
- 수식은 원칙적으로 쓰지 않는다. 계산이 필요하면 숫자를 넣은 단계별 예시로 보여 준다 (통계학입문책 방식).
- 코드는 본문 흐름에 꼭 필요한 짧은 조각만 보여 주고, 긴 코드는 장 끝 부록으로 뺀다. 학생이 코드를 "작성"하는 것이 아니라 에이전트가 만든 코드를 "알아보는" 수준을 목표로 한다.

## 2. 과목의 관점 (내용상 대원칙)

- 이 과목은 AI 에이전트를 밑바닥부터 만드는 과목이 아니라 **이미 만들어진 에이전트(VSCode + Claude Code)를 활용**해 공공데이터를 분석하는 과목이다.
- 학생의 역할은 코딩이 아니라 **지시 → 검증 → 해석**이다. 모든 실습은 "에이전트가 한 일을 학생이 검증하는" 절차를 반드시 포함한다.
- 표준 환경: Windows 노트북(로컬) + VSCode + Claude Code 확장 + uv 기반 Python. Colab 아님. Claude 유료 구독 전제.
- 도구 명칭 표기: "VSCode", "Claude Code", "CLAUDE.md", "SKILL.md", "uv". 이 표기를 통일한다.

## 3. 장 구조

### 이론 장 (N주차 1회차): 파일명 `NN-1_이론_제목.md`

1. `# N주차 1회차. 제목` (h1)
2. 도입 인용구 (blockquote, 실존 인용만. 확인 불가하면 생략)
3. `## 이 장에서 배우는 것`: 도입 이야기 + 학습 목표 목록
4. 로드맵 박스 (아래 HTML 박스 참고)
5. 본문 절 (`## 1.1 …`, `## 1.2 …` 번호는 목차.md를 따름)
6. `## 요약`: 절별 핵심을 줄글로
7. `## 연습문제`: 3개 내외, 컴퓨터 없이 풀 수 있는 것 위주

### 실습 장 (N주차 2회차): 파일명 `NN-2_실습_제목.md`

1. `# N주차 2회차. 제목` (h1)
2. `## 이 실습에서 하는 것`: 목표 + 완성 산출물 미리보기
3. `## 준비물`: 표: 데이터(경로·출처), 도구, 선수 내용(앞 장 참조)
4. 단계별 실습 절 (`## 2.1 …`). 각 단계는 다음 4요소를 갖춘다.
   - (a) 무엇을 왜 하는가
   - (b) **지시문 예시**: 지시문 박스(아래)로 제시. 학생이 그대로 입력할 수 있는 완결된 한국어 지시
   - (c) 예상되는 에이전트의 행동과 결과 (화면에 무엇이 보이는지)
   - (d) **검증**: 결과가 맞는지 학생이 확인하는 구체적 방법
5. `## 검증 체크리스트`: 표 또는 목록
6. `## 자주 겪는 문제`: 증상 → 원인 → 해결
7. `## 제출 과제`: 산출물과 평가 기준

## 4. HTML 박스 (통계학입문책 계승 + 지시문 박스 추가)

로드맵 박스 (남색):
```html
<div style="border:1px solid #d9d9e3;border-left:5px solid #5b6ee1;background:#f7f7fc;padding:12px 16px;margin:18px 0;border-radius:4px;">
<strong>이 장의 로드맵</strong><br> … </div>
```

정의 박스 (파랑):
```html
<div style="border:1px solid #cdd9e5;border-left:5px solid #2f6fb0;background:#f5f9fd;padding:12px 16px;margin:18px 0;border-radius:4px;">
<strong>정의 · 용어명</strong><br> … </div>
```

심화·왜? 박스 (보라):
```html
<div style="border:1px solid #e0d8ea;border-left:5px solid #7a5fa8;background:#faf8fc;padding:12px 16px;margin:18px 0;border-radius:4px;">
<strong>왜 …인가?</strong><br> … </div>
```

유의 박스 (주황):
```html
<div style="border:1px solid #ecd9c6;border-left:5px solid #c77b2f;background:#fdf9f4;padding:12px 16px;margin:18px 0;border-radius:4px;">
<strong>유의 · …</strong><br> … </div>
```

지시문 박스 (초록, 실습 장 전용):
```html
<div style="border:1px solid #cfe3d4;border-left:5px solid #2f8f4e;background:#f4fbf6;padding:12px 16px;margin:18px 0;border-radius:4px;">
<strong>지시문 예시</strong><br>
"…에이전트에게 입력할 지시문 전문…"
</div>
```

## 5. 그림·표·데이터

- 그림은 실제 코드로 생성해 `figures/`에 PNG로 저장하고 `![캡션](figures/파일명.png)`으로 삽입. 생성 코드는 `code/`에 장별로 둔다 (`figNN_*.py`).
- 개념도(에이전트 루프 등)도 matplotlib로 그린다. koreanize_matplotlib 사용, 물결표·em dash를 그림 안 텍스트에도 쓰지 않는다.
- 상자(FancyBboxPatch) 개념도 스크립트에는 `import figfit`을 반드시 추가한다 (code/figfit.py, 상자 글씨를 상자 크기에 맞춰 자동 확대. 수동 fontsize 조정 불필요).
- 모든 그림에는 본문에서 "그림 읽는 법 + 여기서 무엇을 알 수 있나"를 붙인다 (통계학입문책 원칙).
- 표는 Markdown 표. 번호는 "표 N-x", 그림은 "그림 N-x" (N = 주차).
- 예시 데이터는 실제 한국 공공데이터: `data/sigungu_2023.csv`(행안부 주민등록인구 + 합계출산율, 229개 시군구), `data/sigungu_tfr_2013.csv`, `data/income_dist.csv`, `data/grdp_sido.csv`, `data/migration_od_2023.csv`. 추가 수집 시 출처를 README와 장 본문에 명기.
- 통계기관 명칭: "국가데이터처(옛 통계청)"를 첫 등장에 쓰고 이후 "국가데이터처".

## 6. 문장 규칙 (금칙 포함)

- 물결표(~) 금지. 범위는 하이픈: "5-10분", "1-4주".
- em dash(—) 금지. 삽입절은 문장을 나눈다.
- 문체: "-다" 종결의 서술체. 통계학입문책의 차분한 설명조를 따른다. 감탄·과장("놀랍게도", "혁명적인") 금지.
- 각 장은 앞 장과의 연결을 명시한다 ("3주차에서 배운 CLAUDE.md를 떠올려 보자" 식의 상호참조).
- AI 산출물 검증의 중요성을 실습 장마다 최소 한 번 실제 예시로 보여 준다 (에이전트가 틀리는 장면 포함).

## 7. 사실 정확성

- Claude Code 기능 서술은 2026년 기준 공식 문서 기준으로 쓴다: 계획 모드, CLAUDE.md, Skill(SKILL.md, 슬래시 커맨드와 통합), 서브에이전트, MCP, 권한 모드, VSCode 확장(diff 패널, @멘션, 계획 검토).
- 요금·모델명 등 바뀌기 쉬운 사실은 구체 수치를 못 박지 않고 "구독이 필요하다" 수준으로 쓴다.
- 확인되지 않은 통계·사례를 만들어 넣지 않는다. 예시 수치는 실제 데이터에서 계산하거나, 가상의 예임을 명시한다("다섯 개 동네가 있다고 하자").
